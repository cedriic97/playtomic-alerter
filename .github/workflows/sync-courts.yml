name: Sync Court Availability

on:
  schedule:
    # Simple approach: Every 5 minutes always (most reliable)
    - cron: '*/5 * * * *'
  
  # Ermöglicht manuelles Triggern über GitHub UI
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Court Availability Sync
        run: |
          current_hour=$(date -u +%H)
          
          if [ "$current_hour" -ge 9 ] && [ "$current_hour" -le 20 ]; then
            echo "🔥 Peak hours - aggressive monitoring at $(date)"
            sync_type="peak"
          else
            echo "🌙 Off-peak hours - regular monitoring at $(date)"
            sync_type="off-peak"
          fi
          
          echo "🔗 URL: ${{ secrets.SUPABASE_URL }}/functions/v1/sync-availability"
          
          response=$(curl -s -w "%{http_code}" -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/sync-availability" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -H "User-Agent: GitHub-Actions-$sync_type" \
            --max-time 300)
          
          http_code="${response: -3}"
          body="${response%???}"
          
          echo "HTTP Status: $http_code"
          
          if [ "$http_code" != "200" ]; then
            echo "❌ Sync failed with HTTP $http_code"
            echo "Response: $body"
            exit 1
          fi
          
          success=$(echo "$body" | jq -r '.success // false')
          if [ "$success" != "true" ]; then
            echo "❌ Sync returned success=false"
            echo "$body" | jq '.'
            exit 1
          fi
          
          # Extract and display results  
          sync_run_id=$(echo "$body" | jq -r '.syncRunId // ""')
          clubs_synced=$(echo "$body" | jq -r '.stats.clubsSynced // 0')
          slots_found=$(echo "$body" | jq -r '.stats.slotsFound // 0')
          slots_updated=$(echo "$body" | jq -r '.stats.slotsUpdated // 0')
          cancellations=$(echo "$body" | jq -r '.stats.cancellationsDetected // 0')
          errors_count=$(echo "$body" | jq -r '.stats.errors | length')
          
          echo "✅ Sync completed!"
          echo "📊 Run: ${sync_run_id:0:8}... | Clubs: $clubs_synced | Slots: $slots_found | Updated: $slots_updated"
          
          if [ "$cancellations" -gt "0" ]; then
            echo "🚨 $cancellations CANCELLATIONS DETECTED! 🚨"
          fi
          
          if [ "$errors_count" -gt "0" ]; then
            echo "⚠️ $errors_count errors occurred:"
            echo "$body" | jq -r '.stats.errors[]'
          fi